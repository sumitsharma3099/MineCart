<div class="flex justify-center items-center">
        <div class="bg-white p-8 rounded-lg shadow-md w-96">
            <h1 class="text-2xl font-semibold mb-4">Dashboard Stats</h1>

            <!-- Total Orders -->
            <div class="mb-4">
                <p class="text-gray-600">Total Orders</p>
                <p class="text-3xl font-bold text-blue-500"><%= @total_orders %></p>
            </div>

            <!-- Total Sales -->
            <div class="mb-4">
                <p class="text-gray-600">Total Sales</p>
                <p class="text-3xl font-bold text-green-500">$<%= @total_sales %></p>
            </div>

            <!-- Total Customers -->
            <div>
                <p class="text-gray-600">Total Customers</p>
                <p class="text-3xl font-bold text-indigo-500"><%= @total_users %></p>
            </div>
        </div>
    </div>